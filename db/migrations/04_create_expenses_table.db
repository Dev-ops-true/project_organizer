CREATE TABLE expenses(id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, description TEXT VARCHAR(100) NOT NULL, unit_price REAL NOT NULL, quantity REAL NOT NULL, mark_up INTEGER NOT NULL, expense_total REAL, quote_id INTEGER NOT NULL, FOREIGN KEY(quote_id) REFERENCES quotes(id));